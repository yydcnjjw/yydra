# SPDX-License-Identifier: MIT OR Apache-2.0
# Match the registry used by the committed lock; do not depend on user npm config.
registry=https://mirrors.cloud.tencent.com/npm/
audit=false

# Yydra development packages are published to the local registry.
@yydra:registry=http://127.0.0.1:4873/

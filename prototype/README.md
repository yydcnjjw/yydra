# Prototype assets

`reading-queue-reference/` is the known-good Product Workspace produced from
the prototype Distribution and then changed only through Product-owned source,
a new migration, and synchronized committed-generated output.

The directory is evidence, not a second template or production example.
